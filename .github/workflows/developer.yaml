name: developer

on:
  push:
    branches:
      - main
      - '*'

jobs:
  lintBash:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run Bash Linter
        run: bash common/tests/lintShell.sh
